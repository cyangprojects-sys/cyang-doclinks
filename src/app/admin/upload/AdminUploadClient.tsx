// src/app/admin/upload/AdminUploadClient.tsx
"use client";

/**
 * IMPORTANT:
 * Paste your existing upload UI component code here (the one you already built).
 * This file stays "use client" so file inputs + drag/drop work normally.
 */
export default function AdminUploadClient() {
    return (
        <main className="min-h-[70vh] bg-black text-white">
            <div className="mx-auto max-w-3xl px-6 py-12">
                <div className="rounded-3xl bg-white/5 p-8 ring-1 ring-white/10">
                    <h1 className="text-2xl font-semibold">Admin Upload</h1>
                    <p className="mt-2 text-sm text-white/70">
                        Replace this placeholder with your existing upload UI.
                    </p>
                </div>
            </div>
        </main>
    );
}
